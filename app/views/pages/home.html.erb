<h1>Pages#home</h1>
<p>Find me in app/views/pages/home.html.erb</p>

<%= button_to "say Hello Async",  hello_path,remote:true %>

<% if current_user %>
    <p>Hello <%= current_user.email %>!</p>

<% if current_user.admin? %>
There are <%= User.count %> users in the system.
<% end %>

<p>You have successfully authenticated with your Devise account.
You are now ready to interact with this application.
</p>
<%= link_to "Monitor Jobs", sidekiq_web_path %>
<%= link_to "logout", destroy_user_session_path, data: {turbo_method: "delete" } %>
 <% else %>
 <p>You are not signed in.</p>
<%= link_to "Sign Up!", new_user_registration_path %>
<br/>
<%= link_to "login", new_user_session_path %>
<% end %>

